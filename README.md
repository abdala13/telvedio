# Telegram News Platform

## Setup
1. Run `supabase_schema.sql` in Supabase SQL Editor
2. Set environment variables in Vercel
3. For collector: `pip install telethon supabase python-dotenv aiohttp`

## Admin Login
- Username: `admin`
- Password: `admin123`
